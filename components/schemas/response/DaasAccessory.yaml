# components/schemas/Response/DaasAccessory.yaml
type: object
properties:
  id: { type: integer }
  name: { type: string }
  description: { type: string }
  externalId: { type: string }
  productNumber: { type: string }
